 {{!-- <div class="card-body col-12 container-sm border-0"> --}}
  <!-- Card to show previous posts and applicable comments  -->
  <!-- All rendered through handlebar utility functions -->
  {{!-- {{#if blogs.length}} --}}
  {{!-- <div class="card-body blog-list">
    {{#each blogs as |blog|}}
    <div class="row">
      <div class="col-lg-12" id="existing-blogs">
          <h5>On {{blog.created_on}} {{blog.user.username}} blogged: <a href="/blog/{{blog.id}}"></a>
            {{blog.title}} {{blog.content}} </h5>
      </div>
    </div>
    {{/each}}
  </div> --}}
  {{!-- {{/if}} --}}
{{!-- </div> --}}
  <!-- Loop over each object in the projects array -->
{{#each blogs as |blog| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/blog/{{blog.id}}">{{blog.title}}</a>
    </h2>
    <p>Created by {{blog.author_id}} on {{format_date blog.created_on}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{blog.content}}
    </p>
  </div>
</div>
{{/each}}
